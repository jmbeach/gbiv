[package]
name = "gbiv-core"
version = "0.0.0"
edition = "2021"
description = "Internal shared primitives for the gbiv binary and the future roy daemon — workspace-only, not published."
license = "MIT"
repository = "https://github.com/jmbeach/gbiv"
publish = false

[dependencies]
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "time"] }

[dev-dependencies]
serial_test = "3"
tempfile = "3"
